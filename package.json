{
  "name": "react-task-root",
  "private": true,
  "scripts": {
    "build": "npm install --prefix \"Loan Application\" && npm run build --prefix \"Loan Application\"",
    "start": "node scripts/start-render.mjs"
  }
}
